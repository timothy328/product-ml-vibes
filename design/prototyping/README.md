# Prototyping

Build the smallest interactive representation needed to test a risky
assumption. Label prototype fidelity and limitations, then connect findings to
a decision about learning, iterating, or stopping.
